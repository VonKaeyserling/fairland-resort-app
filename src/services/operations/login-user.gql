mutation LoginUser($data: LoginUserInput!) {
  loginUser(data: $data) {
    token
    user {
      email
      firstName
      id
      lastName
      verified
      userName
      avatar
    }
  }
}
